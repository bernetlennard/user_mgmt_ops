apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-mgmt.componentName" (dict "ctx" $ "component" "backend") }}
  labels:
    {{- include "user-mgmt.labels" (dict "ctx" $ "component" "backend") | nindent 4 }}
spec:
  type: ClusterIP
  # Stated rather than defaulted: no affinity means requests go round-robin over the ready
  # endpoints, while ClientIP would pin each client to one replica. Safe because the API is
  # stateless -- authentication is a JWT in the request, not a server-side session.
  sessionAffinity: None
  # The Actuator port (backend.management.port) is deliberately not published here; only the
  # kubelet talks to it, on the pod IP.
  selector:
    {{- include "user-mgmt.selectorLabels" (dict "ctx" $ "component" "backend") | nindent 4 }}
  ports:
    - name: backend
      port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.port }}
